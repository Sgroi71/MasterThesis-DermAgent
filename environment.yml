name: dino310
channels:
  - conda-forge
dependencies:
  - python=3.10
  - pip>=24.0
  - setuptools>=68
  - wheel>=0.41
  - ninja>=1.11
  - pkg-config
  # (Optional GPU) Uncomment the next line on CUDA hosts, or keep CPU-only.
  # - cuda-toolkit=12.1.*      # keep aligned with your Torch CUDA build

  - pip:
      # ---- Your pyproject deps (kept/pinned) ----
      # Core runtime
      - numpy==1.26.4
      - pandas==2.2.3
      - pillow==11.2.1
      - scipy==1.15.2
      - scikit-image==0.25.2
      - matplotlib==3.10.1

      # DL / Vision
      - torch==2.6.0
      - torchvision==0.21.0
      - timm==1.0.15
      - transformers==4.51.3
      - tokenizers==0.21.1
      - safetensors==0.5.3
      - huggingface-hub==0.30.2

      # XAI
      - captum==0.8.0

      # Web/API & UI
      - fastapi==0.115.12
      - uvicorn==0.34.1
      - gradio==5.25.2
      - gradio_client==1.8.0
      - python-multipart==0.0.20
      - jinja2==3.1.6
      - aiofiles==24.1.0

      # LLM / LangChain stack
      - langchain-core==0.3.52
      - langchain-openai==0.3.13
      - langgraph==0.3.30
      - langgraph-checkpoint==2.0.24
      - langgraph-prebuilt==0.1.8
      - langgraph-sdk==0.1.61
      - langsmith==0.3.31
      - openai==1.74.0
      - tiktoken==0.9.0

      # Utils
      - python-dotenv==1.1.0
      - pydantic==2.11.3
      - orjson==3.10.16
      - requests==2.32.3
      - httpx==0.28.1
      - anyio==4.9.0
      - tenacity==9.1.2
      - tqdm==4.67.1
      - pyyaml==6.0.2
      - regex==2024.11.6
      - rich==14.0.0

      # ---- Vision/Detectron2 friends & COCO ----
      - fvcore>=0.1.5
      - iopath>=0.1.10
      - yacs>=0.1.8
      - termcolor>=2.3
      - pycocotools>=2.0.7

      # IMPORTANT: OpenCV pin compatible with NumPy 1.26.x (to keep Captum happy)
      - opencv-python==4.9.0.80